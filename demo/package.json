{
  "private": true,
  "devDependencies": {
    "helic-react-config": "file:.."
  },
  "scripts": {
    "postinstall": "react-config svn-install",
    "preuninstall": "react-config svn-uninstall",
    "lint-scripts": "react-config lint-scripts",
    "lint-styles": "react-config lint-styles"
  }
}
